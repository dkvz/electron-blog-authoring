{
  "name": "electron-blog-authoring",
  "productName": "Blog Authoring",
  "version": "1.1.0",
  "description": "Electron authoring tool for my blog",
  "main": "electron/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel -d lib/ components/",
    "start": "npm run compile && NODE_ENV=dev electron .",
    "start-win": "npm run compile&&set NODE_ENV=dev&&electron ."
  },
  "author": "dkvz@dkvz.eu",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-preact": "^1.1.0",
    "braces": ">=2.3.1",
    "electron": "^4.0.6",
    "electron-packager": "^12.1.0",
    "lodash": ">=4.17.19",
    "preact": "^8.4.0"
  }
}
